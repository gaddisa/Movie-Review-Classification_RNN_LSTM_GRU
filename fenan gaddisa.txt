<div class="p-4 bg-gradient-to-br from-gray-50 to-blue-50 rounded-lg shadow-lg border border-gray-200">
  <div class="flex justify-between items-center pb-2 border-b border-gray-200 mb-3">
    <h2 class="text-xl font-bold text-gray-800 flex items-center">
      <i class="fas fa-paperclip mr-2 text-blue-600"></i>
      {{ $isEdit ? __('Edit Attachment') : __('Add New Attachment') }}
    </h2>
    <button wire:click="$dispatch('closeModal')"
            class="text-gray-500 hover:text-red-600 transition duration-200 transform hover:scale-110 p-1">
      <i class="fas fa-times-circle text-xl"></i>
    </button>
  </div>

  <form wire:submit.prevent="saveAttachment" class="space-y-4">
    <!-- DCT Selection -->
    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">
        <i class="fas fa-project-diagram mr-1 text-gray-500"></i> @lang('DCT') *
      </label>
      <select wire:model="dct_id" class="w-full border border-gray-300 rounded-lg px-2 py-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition text-sm">
        <option value="">-- Select DCT --</option>
        @foreach ($this->dcts as $dct)
          <option value="{{ $dct->id }}">{{ $dct->name }}</option>
        @endforeach
      </select>
      @error('dct_id')
        <span class="text-xs text-red-500 flex items-center mt-1">
          <i class="fas fa-exclamation-circle mr-1"></i> {{ $message }}
        </span>
      @enderror
    </div>

    <!-- Name (full width) -->
    <div>
      <label class="block text-sm font-medium text-gray-700 mb-1">
        <i class="fas fa-signature mr-1 text-gray-500"></i> @lang('Name') *
      </label>
      <textarea wire:model="name" rows="3" class="w-full border border-gray-300 rounded-lg px-2 py-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition resize-y text-sm"></textarea>
      @error('name')
        <span class="text-xs text-red-500 flex items-center mt-1">
          <i class="fas fa-exclamation-circle mr-1"></i> {{ $message }}
        </span>
      @enderror
    </div>

    <!-- Combined row: Type, Status, Required -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-3 text-sm">
      <!-- Type -->
      <div>
        <label class="font-medium text-gray-700 mb-1 flex items-center">
          <i class="fas fa-tag mr-1 text-gray-500"></i> @lang('Type') *
        </label>
        <select wire:model="attachement_type" class="w-full border border-gray-300 rounded-lg px-2 py-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition">
          <option value="DOCUMENT">DOCUMENT</option>
          <option value="TABLE">TABLE</option>
          <option value="IMAGE">IMAGE</option>
          <option value="VIDEO">VIDEO</option>
        </select>
        @error('attachement_type')
          <span class="text-xs text-red-500 flex items-center mt-1">
            <i class="fas fa-exclamation-circle mr-1"></i> {{ $message }}
          </span>
        @enderror
      </div>

      <!-- Status -->
      <div>
        <label class="font-medium text-gray-700 mb-1 flex items-center">
          <i class="fas fa-toggle-on mr-1 text-gray-500"></i> @lang('Status') *
        </label>
        <select wire:model="status" class="w-full border border-gray-300 rounded-lg px-2 py-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition">
          <option value="active">Active</option>
          <option value="inactive">Inactive</option>
        </select>
        @error('status')
          <span class="text-xs text-red-500 flex items-center mt-1">
            <i class="fas fa-exclamation-circle mr-1"></i> {{ $message }}
          </span>
        @enderror
      </div>

      <!-- Is Required -->
      <div>
        <label class="font-medium text-gray-700 mb-1 flex items-center">
          <i class="fas fa-asterisk mr-1 text-gray-500"></i> @lang('Required') *
        </label>
        <select wire:model="is_required" class="w-full border border-gray-300 rounded-lg px-2 py-1 focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition">
          <option value="Optional">Optional</option>
          <option value="Mandatory">Mandatory</option>
        </select>
        @error('is_required')
          <span class="text-xs text-red-500 flex items-center mt-1">
            <i class="fas fa-exclamation-circle mr-1"></i> {{ $message }}
          </span>
        @enderror
      </div>
    </div>

    <!-- Note about images and videos -->
    <!-- Note about images and videos -->
<div class="bg-yellow-50 border border-yellow-200 rounded-lg p-3 text-sm">
    <p class="text-yellow-700">
        <strong>Note:</strong> For picture and video attachments, institutions/TVETs can submit a PDF file containing links to their videos and photos.
    </p>
</div>

    <div class="flex justify-end space-x-3 pt-2">
      <button wire:click="$dispatch('closeModal')" type="button" class="px-3 py-1 bg-gray-100 text-gray-700 rounded-lg hover:bg-gray-200 transition flex items-center text-sm">
        <i class="fas fa-times mr-2"></i> @lang('Cancel')
      </button>
      <button type="submit" class="px-4 py-1 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition flex items-center text-sm" wire:loading.attr="disabled">
        <span wire:loading.remove wire:target="saveAttachment">
          <i class="fas fa-save mr-2"></i> {{ $isEdit ? __('Update') : __('Save') }}
        </span>
        <span wire:loading wire:target="saveAttachment">
          <i class="fas fa-spinner fa-spin mr-2"></i> @lang('Saving...')
        </span>
      </button>
    </div>
  </form>
</div>
